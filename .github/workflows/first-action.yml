name: First Workflow in the action
on: workflow_dispatch
jobs :
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "First output"
          echo "Second output"
    
      - name: Print goodbye
        run : echo "good BYE!"
